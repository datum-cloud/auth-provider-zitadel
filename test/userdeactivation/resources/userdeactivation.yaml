apiVersion: iam.miloapis.com/v1alpha1
kind: UserDeactivation
metadata:
  name: human-user-deactivation-1
  annotations:
    kubernetes.io/description: "Deactivation request for human-user (e2e test)"
spec:
  # Reference to the User being deactivated (required)
  userRef:
    name: "human-user"

  # Internal reason for deactivation (required)
  reason: "E2E Test"

  # Detailed internal description for the deactivation (optional)
  description: "Automatically created by chainsaw e2e test to validate UserDeactivation controller."

  # Who initiated the deactivation (required)
  deactivatedBy: "ci@example.com" 